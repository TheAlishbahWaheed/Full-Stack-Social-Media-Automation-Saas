"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";import Link from"next/link";import Field from"../../components/Field";import{api,save}from"../../lib/api";
export default function Login(){const r=useRouter();const[e,setE]=useState("");const[p,setP]=useState("");const[err,setErr]=useState("");async function submit(x:FormEvent){x.preventDefault();try{const d=await api<{access_token:string}>("/auth/login",{method:"POST",body:JSON.stringify({email:e,password:p})});save(d.access_token);r.push("/dashboard")}catch(x){setErr(x instanceof Error?x.message:"Login failed")}}return <main className="flex min-h-screen items-center justify-center p-5"><form onSubmit={submit} className="w-full max-w-md space-y-5 rounded-2xl bg-white p-8 shadow-xl"><h1 className="text-3xl font-bold">Welcome back</h1>{err&&<p className="rounded-xl bg-red-50 p-3 text-red-700">{err}</p>}<Field label="Email" type="email" value={e} onChange={x=>setE(x.target.value)} required/><Field label="Password" type="password" value={p} onChange={x=>setP(x.target.value)} required/><button className="w-full rounded-xl bg-indigo-600 py-3 font-semibold text-white">Login</button><p className="text-center text-sm">No account? <Link className="text-indigo-600" href="/register">Register</Link></p></form></main>}
